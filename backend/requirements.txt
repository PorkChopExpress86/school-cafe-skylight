fastapi==0.141.1
# FastAPI permits Starlette 1.x, whose TestClient deprecates this project's
# pinned httpx dependency in favour of a separate httpx2 package. Keep the
# current httpx-based test client until this repository deliberately migrates.
starlette==0.52.1
uvicorn[standard]==0.52.1
python-dotenv==1.2.2
# Pinned to a commit rather than a branch: pyskylight is an unofficial,
# reverse-engineered client, so an upstream push shouldn't be able to change
# this app's behaviour without an explicit bump here.
pyskylight @ git+https://github.com/joshuaswarren/pyskylight@69e4576b9035d71aacda9ade7a4afea05a663e94
